# Supabase — https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Social login providers — set these up in Supabase Dashboard > Authentication > Providers
# Google: console.cloud.google.com > APIs & Services > Credentials
# Apple:  developer.apple.com > Certificates, Identifiers & Profiles > Services IDs
# (client id/secret for each provider are configured in the Supabase dashboard,
#  not as env vars here — Supabase handles the OAuth redirect for you)
